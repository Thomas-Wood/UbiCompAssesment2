const colours = {
    background: '#ffffff',
    card: '#bad9b5',
}

export default {
    colours: colours,
    container: {
        flex: 1,
        backgroundColor: colours.background,
        alignItems: 'center',
        justifyContent: 'center',
      },
    card: {
        flex: 1,
        backgroundColor: colours.card,
        alignItems: 'center',
        justifyContent: 'center',
    },
}